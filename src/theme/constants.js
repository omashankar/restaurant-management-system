/** localStorage key for cached customer theme (per restaurant slug). */
export const CUSTOMER_THEME_STORAGE_KEY = "rms-customer-theme-v1";

export const DEFAULT_PRIMARY = "#FF6B35";
export const DEFAULT_SECONDARY = "#FF9F1C";

export const COLOR_MODES = /** @type {const} */ (["light", "dark"]);
